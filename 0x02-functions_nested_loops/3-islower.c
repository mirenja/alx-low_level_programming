#include "main.h"
/**
* int _islower - Checks lowercase
* C: has value to be compared 
* Return: 1 if lowercase
* return: 0 if otherwise
*/
int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
	{
	return (1);
	}
	else
	{
	return (0);
	}
}
